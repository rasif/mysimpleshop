import React from 'react';

const Header = () => {
    console.log('Header');
    return (
        <header className="header">
            <p className="header__title">Web Shop</p>
        </header>
    );
};

export default Header;
